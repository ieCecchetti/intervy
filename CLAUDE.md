# intervy-playground

## Skill files
Skills live under `.claude/skills/` in this repo. Always edit the local copies here — never touch `~/.claude/skills/`.

## Git
- Commit and push all changes
- No co-author trailers (no `Co-Authored-By` lines)
- Git user: ieCecchetti
- Do not reference `.claude` in commit messages
